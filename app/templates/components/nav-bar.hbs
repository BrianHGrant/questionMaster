<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index' class="navbar-brand"}}questionMaster{{/link-to}}
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <li>
          {{#link-to 'index' class="nav-item nav-link"}}About{{/link-to}}
        </li>
        {{#if userAuth.ifAuth}}
          <li>
            {{#link-to 'index' class="nav-item nav-link our-nav"}}Logout{{/link-to}}
          </li>
          <li>
            {{#link-to 'index' class="nav-item nav-link our-nav"}}User Profile (<span id="userName">{{userAuth.authUser.name}}</span>){{/link-to}}
          </li>
        {{else}}
          <li>
            {{#link-to 'login' class="nav-item nav-link our-nav"}}Login/Sign Up{{/link-to}}
          </li>
        {{/if}}
      </ul>
      <form class="navbar-form navbar-right">
        <input type="text" class="form-control" placeholder="Search...">
      </form>
    </div>
  </div>
</nav>
