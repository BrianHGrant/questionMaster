import { avatarDisplay } from 'question-master/helpers/avatar-display';
import { module, test } from 'qunit';

module('Unit | Helper | avatar display');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = avatarDisplay([42]);
  assert.ok(result);
});
